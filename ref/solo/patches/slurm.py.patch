--- a/relevance/src/slurm.py
+++ b/relevance/src/slurm.py
@@ -100,8 +100,10 @@ def init_distributed_mode(params):
         params.n_nodes = 1
         params.node_id = 0
         params.local_rank = 0
         params.global_rank = 0
-        params.world_size = n_gpu
-        params.n_gpu_per_node = n_gpu
+        # Force world_size = 1 for single-process training (even with multiple GPUs visible)
+        # The training script (finetune_table_retr.py) requires world_size == 1
+        params.world_size = 1
+        params.n_gpu_per_node = 1
         params.is_distributed = False
 
     # define whether this is the master process / if we are in distributed mode
